# 景区信息管理系统

> 数据结构课程实验项目

基于图算法实现的景区信息管理系统，提供景点导航、路径规划、电路铺设等功能。

## 目录

- [实验目的](#实验目的)
- [系统功能](#系统功能)
- [编译运行](#编译运行)
- [核心算法](#核心算法)
- [数据格式](#数据文件格式)
- [使用示例](#使用示例)

---

## 实验目的

通过"景区信息管理系统"的编程实践，学习图的遍历、Dijkstra 算法、最小生成树算法、Prim 算法和它们的编程应用。

- 掌握图的定义和图的存储结构
- 掌握图的创建方法
- 掌握图的两种遍历方法
- 理解迪杰斯特拉（Dijkstra）算法
- 理解最小生成树的概念和普里姆（Prim）算法
- 掌握文件操作
- 使用 C++ 语言，利用图的数据结构，开发景区信息管理系统

## 业务背景

现有一个景区，景区里面有若干个景点，其景点分布如图 1 所示，并且景点之间满足以下条件：

1. 某些景点之间铺设了道路（相邻）
2. 这些道路都可以双向行驶的（无向图）
3. 从任意一个景点出发都可以游览整个景区（连通图）

开发景区信息管理系统，对景区的信息进行管理。使用图的数据结构来保存景区景点信息，为用户提供创建图、查询景点信息、旅游景点导航、搜索最短路径、铺设电路规划等功能。

## 景点数据

景区的数据包含景点信息和景点之间的道路信息，分别由两个文本文件存储。

### 景点信息（Vex.txt）

景点信息主要包含景点的编号、景点的名字和相关介绍等内容：

| 编号 | 名字 | 介绍 |
|------|------|------|
| 0 | A 区 | A 区是景区的主要入口，有停车场和游客中心 |
| 1 | B 区 | B 区有儿童游乐场和休息区 |
| 2 | C 区 | C 区有历史博物馆和文化展览 |
| 3 | D 区 | D 区有自然风光和观景台 |
| 4 | E 区 | E 区有植物园和花卉展览 |
| 5 | F 区 | F 区有水上乐园和沙滩 |
| 6 | G 区 | G 区有山顶观景台和索道 |

### 道路信息（Edge.txt）

道路信息通过一个三元组的形式来表示，分别是景点 1（起点）的名字，景点 2（终点）的名字，两个景点之间的距离长度：

| 景点 1 | 景点 2 | 距离（m） |
|-------|-------|-----------|
| A 区 | C 区 | 700 |
| A 区 | E 区 | 1000 |
| A 区 | F 区 | 600 |
| B 区 | C 区 | 1000 |
| B 区 | G 区 | 1000 |
| C 区 | D 区 | 400 |
| D 区 | E 区 | 300 |
| D 区 | G 区 | 400 |
| E 区 | F 区 | 600 |
| F 区 | G 区 | 500 |

## 系统功能

整个景区信息管理包含 6 个功能模块：

| 功能 | 描述 | 核心算法 |
|------|------|---------|
| 创建景区地图 | 从文件读取景点和道路信息 | 图的构建 |
| 查询景点信息 | 查看景点详情及相邻景点 | 邻接表遍历 |
| 展示景区地图 | 显示所有景点和道路 | 图遍历 |
| 旅游导航推荐 | 查找游览所有景点的无回路路线 | DFS |
| 搜索最近路线 | 计算两点间最短路径 | Dijkstra |
| 铺设电路规划 | 设计最短电路铺设方案 | Prim |

---

### 1. 创建图

**输入：** 从 Vex.txt 文件中读取景点信息，从 Edge.txt 文件中读取道路信息。

**处理：** 根据读取的景点信息创建景区景点图。

**输出：** 创建成功，一次输出：
1. 顶点数目
2. 顶点编号
3. 顶点名字
4. 边两端的顶点编号
5. 边的权值

创建失败，输出失败的提示信息。

### 2. 查询景点

**输入：** 想要查询的景点的编号

**处理：** 根据输入的景点编号，查询该景点及相邻景点的信息。

**输出：**
1. 景点名字
2. 景点介绍
3. 相邻景点的名字
4. 到达相邻景区的路径长度

### 3. 旅游景点导航

**输入：** 起始景点的编号

**处理：** 使用深度优先搜索（DFS）算法，查找以该景点为起点，无回路游览整个景区所有景点的所有可能路线（哈密顿路径）。优先选择距离最短的边进行探索。

**输出：**
1. 所有符合要求的导航路线
2. 每条路线的总距离

### 4. 搜索最短路径

**输入：** 起始景点的编号和终点的编号

**处理：** 使用迪杰斯特拉算法，求得从起始景点到终点之间的最短路径，计算路径总长度。

**输出：**
1. 最短路线
2. 路径总长度

### 5. 铺设电路规划

**输入：** 景区的所有景点信息和道路信息

**处理：** 根据景区景点图使用普里姆（Prim）算法构造最小生成树，设计出一套铺设线路最短，但能满足每个景点都能通电的方案。

**输出：**
1. 需要铺设电路的道路
2. 每条道路铺设电路的长度
3. 铺设电路的总长度

## 项目架构

### 技术栈
- **编程语言：** C++17
- **构建工具：** CMake
- **数据结构：** 图（邻接表存储）

### 项目结构

```
AttractionManagement/
├── include/               # 头文件目录
│   ├── graph.hpp         # 图类定义
│   └── tourism.hpp       # 旅游管理类定义
├── src/                  # 源文件目录
│   ├── Main_CLI.cpp      # 程序入口
│   ├── graph.cpp         # 图类实现
│   └── tourism.cpp       # 旅游管理类实现
├── data/                 # 数据文件目录
│   ├── Vex.txt           # 景点信息
│   └── Edge.txt          # 道路信息
├── bin/                  # 可执行文件目录
├── CMakeLists.txt        # CMake 配置文件
└── README.md             # 项目说明文档
```

### 分层架构

#### 视图层（UI 层）
- **Main_CLI.cpp** - 程序入口，命令行交互界面

#### 服务层（业务逻辑层）
- **tourism.hpp/cpp** - 旅游管理类，业务逻辑处理
  - 创建图
  - 查询景点信息
  - 景点导航
  - 最短路径规划
  - 铺设电路规划

#### 算法层（数据结构层）
- **graph.hpp/cpp** - 图类，图算法实现
  - 建图操作
  - 顶点信息查询
  - 深度优先搜索（DFS）- 查找遍历所有景点的无回路路线
  - 单源点最短路径 - Dijkstra 算法
  - 最小生成树 - Prim 算法

## 编译运行

### 环境要求
- C++17 编译器
- CMake 3.10+

### 编译步骤

```bash
# 创建构建目录
mkdir build
cd build

# 配置项目（Windows 使用 MinGW）
cmake -S .. -B . -G "MinGW Makefiles"

# 编译项目
cmake --build .

# 运行程序
./run_cli.exe
```

## 核心算法

### DFS（深度优先搜索）
用于旅游景点导航，查找无回路游览整个景区所有景点的路线（哈密顿路径）。

**算法思路：**
1. 从起始顶点开始，标记为已访问
2. 递归访问所有未访问的邻接顶点（优先选择最短边）
3. 当路径长度等于景点总数时，保存该路径
4. 回溯继续探索其他分支

**特点：** 返回所有能遍历全部景点的无回路路线

**时间复杂度：** O(V!)，最坏情况需要枚举所有排列

### Dijkstra 算法
用于搜索最短路径，计算两点间的最短路径和距离。

**算法思路：**
1. 初始化距离数组，起点距离为 0，其余为无穷大
2. 每次选择未访问顶点中距离最小的顶点
3. 对该顶点的邻接顶点进行松弛操作
4. 重复直到所有顶点都被访问

**时间复杂度：** O(V²)，可优化至 O(E log V)（使用优先队列）

### Prim 算法
用于铺设电路规划，构造最小生成树。

**算法思路：**
1. 从任意顶点开始，将其加入最小生成树
2. 在所有连接已访问和未访问顶点的边中选择权值最小的边
3. 将该边对应的未访问顶点加入最小生成树
4. 重复直到所有顶点都被访问

**时间复杂度：** O(V²)，可优化至 O(E log V)（使用优先队列）

## 算法复杂度分析

| 算法 | 时间复杂度 | 空间复杂度 | 适用场景 |
|------|-----------|-----------|---------|
| DFS（哈密顿路径） | O(V!) | O(V) | 遍历所有景点的无回路路线 |
| Dijkstra | O(V²) | O(V) | 单源最短路径 |
| Prim | O(V²) | O(V) | 最小生成树 |

## 数据文件格式

### Vex.txt 格式
```
<编号> <名称> <描述>
```
- 编号：整数，从 0 开始
- 名称：字符串，不含空格
- 描述：字符串，可包含空格

示例：
```
0 A 区 A 区是景区的主要入口，有停车场和游客中心
```

### Edge.txt 格式
```
<起点名称> <终点名称> <权重>
```
- 起点名称：字符串，需与 Vex.txt 中的名称一致
- 终点名称：字符串，需与 Vex.txt 中的名称一致
- 权重：整数，表示距离（单位：米）

示例：
```
A 区 C 区 700
```

## 使用示例

### 运行程序
```
====================================
      景区信息管理系统
====================================
  1. 创建景区地图
  2. 查询景点信息
  3. 展示景区地图
  4. 旅游导航推荐
  5. 搜索最近路线
  6. 铺设电路规划
  0. 退出系统
====================================
请选择操作（0-6）：
```

### 查询景点信息
```
请输入景点编号：0
========== 景点信息 ==========
编号：0
名称：A 区
描述：A 区是景区的主要入口，有停车场和游客中心

相邻景点：
  C 区（距离：700m）
  E 区（距离：1000m）
  F 区（距离：600m）
==============================
```

### 旅游导航推荐
```
请输入起始景点编号：0
========== 旅游路径 ==========
从 A 开始游览

共有 4 条游览路线：

路线 1：A -> F -> G -> D -> E -> C -> B
  距离：3200m
路线 2：A -> F -> G -> B -> C -> D -> E
  距离：3800m
...
==============================
```

### 搜索最短路径
```
请输入起点编号：0
请输入终点编号：6
========== 最短路径 ==========
从 A 区 到 G 区

最短路线：
  A 区 -> F 区 -> G 区
最短距离：1100m
==============================
```

### 铺设电路规划
```
========== 电路铺设规划 ==========
最小生成树方案：

需要铺设电路的道路：
  C 区 <-> D 区（长度：400m）
  D 区 <-> E 区（长度：300m）
  D 区 <-> G 区（长度：400m）
  A 区 <-> F 区（长度：600m）
  F 区 <-> G 区（长度：500m）
  B 区 <-> G 区（长度：1000m）

总铺设长度：3200m
==================================
```

## API 参考

### Graph 类

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `getVexNum()` | 无 | int | 获取顶点数量 |
| `getEdgeNum()` | 无 | int | 获取边数量 |
| `addVex(vex)` | Vex | void | 添加顶点 |
| `getVex(index)` | int | Vex | 获取顶点信息 |
| `addEdge(v1, v2, weight)` | int, int, int | void | 添加边 |
| `getWeight(v1, v2)` | int, int | int | 获取边权重 |
| `getAdjList(v)` | int | vector<ArcNode>& | 获取邻接表 |
| `calcPathWeight(path)` | vector<int> | int | 计算路径权重 |
| `DFSTraverse(start)` | int | vector<vector<int>> | 深度优先遍历，返回所有遍历全部景点的路径 |
| `Prim()` | 无 | vector<Edge> | Prim 最小生成树 |
| `dijkstra(start, end)` | int, int | vector<int> | Dijkstra 最短路径 |

### Tourism 类

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `createGraph(vexPath, edgePath)` | string, string | int | 从文件创建图 |
| `getSpotInfo()` | 无 | int | 查询景点信息 |
| `showGraph()` | 无 | int | 展示图信息 |
| `TravelPath()` | 无 | int | 旅游路径规划 |
| `findShortPath()` | 无 | int | 查找最短路径 |
| `designMinPath()` | 无 | int | 电路铺设规划 |

## 扩展建议

1. **算法优化**：使用优先队列优化 Dijkstra 和 Prim 算法，将时间复杂度从 O(V²) 优化至 O(E log V)
2. **GUI 界面**：可使用 Qt 等框架开发图形用户界面
3. **数据持久化**：支持数据库存储景点和道路信息
4. **路径可视化**：绘制景区地图，可视化显示路径规划结果
5. **多语言支持**：添加国际化支持，支持多语言界面
